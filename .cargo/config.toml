[target.'cfg(target_arch = "aarch64")']
# Candle's GEMM backend emits AArch64 half-precision vector instructions. The
# generic Termux target does not advertise them even on supported Snapdragon CPUs.
rustflags = ["-C", "target-feature=+fp16"]
